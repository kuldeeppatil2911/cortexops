{
  "name": "cortexops",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack incident management system",
  "scripts": {
    "start": "node backend/server.js",
    "start:production": "node backend/server-prod.js",
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}
